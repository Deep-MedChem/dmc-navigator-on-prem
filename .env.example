# Copy to .env (install_navigator.sh does this for you) and edit.
# docker compose reads this file automatically.

# ── Container image ───────────────────────────────────────────────────────────
# Exact shared production repository published by Deep-MedChem in AWS ECR.
# `navigator login` uses the active AWS source identity to assume
# arn:aws:iam::815935788477:role/navigator-onprem-pull automatically. The temporary
# credentials are not persisted. The current image platform is linux/amd64.
DMC_NAV_IMAGE=815935788477.dkr.ecr.us-east-1.amazonaws.com/on-prem/dmc-navigator
# `stable` advances only after a reviewed, tested on-prem release succeeds.
DMC_NAV_IMAGE_TAG=stable

# ── Local paths (relative to this repo, or absolute) ──────────────────────────
# Workflow run state (created on demand).
DMC_NAV_RUNS_DIR=./runs
# Your signed license file. `navigator update-license <path>` installs it here.
DMC_NAV_LICENSE_FILE=./license.json
# Optional directory for your own reaction/synthon TSVs and target JSONs.
DMC_NAV_INPUTS_DIR=./inputs

# ── Misc ──────────────────────────────────────────────────────────────────────
# 1 = show Python tracebacks (debugging). Leave empty for clean one-line errors.
DMC_NAV_DEBUG=

# ── Set automatically by install_navigator.sh — do not copy a value by hand ───
# DMC_NAV_UID / DMC_NAV_GID: your `id -u` / `id -g`, recorded at install time so
# the container writes ./runs and ./inputs as your user instead of root.
# (Without them, docker-compose.yml falls back to 1000:1000.)
