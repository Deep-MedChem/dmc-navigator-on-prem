# Local, machine-specific, or secret — never commit.
.env
license.json
db_install.key
*.pem
*.tar
*.tar.gz

# Runtime data (created by the installer / at run time).
runs/
inputs/
databases/

# Cruft
__pycache__/
*.pyc
.DS_Store

# Local test scratchpad artifacts (the notebook itself IS tracked)
_local_license.json
.ipynb_checkpoints/
.test-home/
